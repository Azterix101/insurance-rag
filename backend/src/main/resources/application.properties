# Server Configuration
server.port=8080

# PostgreSQL Connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/insurance_rag
spring.datasource.username=postgres
spring.datasource.password=local_password_123
spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.mode=always

# AWS credentials for LocalStack (overridden mock values)
aws.s3.endpoint=http://localhost:4566
aws.accessKeyId=mock
aws.secretAccessKey=mock
aws.region=us-east-1
